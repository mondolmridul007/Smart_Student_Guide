<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartstudentguide">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartStudentGuide">
        <activity android:name=".Forget_Password"></activity>
        <activity android:name=".Student_Register" />
        <activity android:name=".Student_Login" />
        <activity android:name=".FullImageView" />
        <activity
            android:name=".ebook.Ebook"
            android:parentActivityName=".studentView.studentMain" />
        <activity android:name=".studentView.Developer" />
        <activity android:name=".studentView.studentMain" />
        <activity android:name=".notice.DeleteNotice" />
        <activity android:name=".faculty.UpdateTeacher" />
        <activity android:name=".faculty.AddTeacher" />
        <activity android:name=".faculty.UpdateFaculty" />
        <activity android:name=".UploadPdf" />
        <activity android:name=".UploadImage" />
        <activity android:name=".notice.UploadNotice" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>